# Exercise 01 - String Extensions

Create two extension methods for `string?`:

1. `IsPresent()` returns true when the string is not null, empty, or whitespace.
2. `OrDefault(string fallback)` returns the original string when present, otherwise returns the fallback.

# Exercise 02 - Email Validation

Improve `IsEmailLike()`.

Requirements:
- Reject null or whitespace.
- Require exactly one `@`.
- Require at least one `.` after `@`.
- Keep the method simple. Do not build a full RFC email validator.
